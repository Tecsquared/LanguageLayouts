---
---

<section class="smarter-section">
  <div class="container">
    <h2 class="section-title reveal">Smarter not Harder</h2>
    <p class="section-subtitle reveal">
      Learn smarter, not harder. Language Layouts templates are designed
      around proven learning principles — retrieval practice, spacing, elaboration,
      generation, and desirable difficulty.
    </p>

    <div class="comparison-widget reveal" style="--reveal-scale: 0.96;">
      <div class="comparison-slider" id="comparison-slider">
        <!-- Smarter image (background, full width) -->
        <div class="slider-img slider-img-smarter">
          <img src="/images/smarter-illustration.png" alt="Smarter learning — focused, effective study" draggable="false" />
        </div>

        <!-- Harder image (foreground, clipped by slider) -->
        <div class="slider-img slider-img-harder" id="slider-harder">
          <img src="/images/learner-illustration.png" alt="Harder learning — overwhelmed, repetitive study" draggable="false" />
        </div>

        <!-- Label inside the card — switches based on slider position -->
        <span class="slider-label slider-label-harder" id="slider-label-harder">HARDER</span>
        <span class="slider-label slider-label-smarter" id="slider-label-smarter" style="display: none;">SMARTER</span>

        <!-- Divider line + handle -->
        <div class="slider-divider" id="slider-divider">
          <div class="slider-line"></div>
          <div class="slider-handle">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M6 10L2 10M2 10L5 7M2 10L5 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M14 10L18 10M18 10L15 7M18 10L15 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </div>
        </div>
      </div>

      <div class="comparison-labels">
        <div class="comparison-label label-harder">
          <span class="label-dot harder-dot"></span>
          <div class="label-content">
            <span class="label-title">Harder</span>
            <span class="label-desc">Going over the same stuff again and again.</span>
          </div>
        </div>
        <div class="comparison-label label-smarter">
          <div class="label-content" style="text-align: right;">
            <span class="label-title">Smarter</span>
            <span class="label-desc">Using simple techniques that make your brain work a little better.</span>
          </div>
          <span class="label-dot smarter-dot"></span>
        </div>
      </div>

      <p class="slider-hint hand-note" aria-hidden="true">slide me side to side</p>
    </div>
  </div>
</section>

<style>
  .comparison-widget {
    max-width: 700px;
    margin: 40px auto 0;
  }

  .comparison-slider {
    position: relative;
    width: 100%;
    height: 400px;
    border-radius: var(--radius-lg);
    border: 2px solid var(--ink);
    overflow: hidden;
    background: var(--card);
    box-shadow: var(--shadow-soft);
    cursor: ew-resize;
    user-select: none;
    -webkit-user-select: none;
    margin-bottom: 24px;
  }

  .slider-img {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--card);
  }

  .slider-img img {
    max-width: 60%;
    max-height: 80%;
    object-fit: contain;
    pointer-events: none;
  }

  .slider-img-harder {
    clip-path: inset(0 0% 0 0);
    z-index: 2;
  }

  .slider-img-smarter {
    z-index: 1;
  }

  .slider-label {
    position: absolute;
    bottom: 20px;
    z-index: 5;
    font-family: var(--font-heading);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 6px 14px;
    border-radius: 999px;
    border: 2px solid var(--ink);
    box-shadow: 2px 2px 0 var(--ink);
  }

  .slider-label-harder {
    left: 20px;
    background: var(--ink);
    color: var(--paper);
    transition: opacity 0.3s;
  }

  .slider-label-smarter {
    right: 20px;
    background: var(--terra);
    color: var(--paper);
    transition: opacity 0.3s;
  }

  .slider-divider {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 100%;
    z-index: 10;
    width: 3px;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
  }

  .slider-line {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 3px;
    background: var(--terra);
  }

  .slider-handle {
    position: relative;
    width: 42px;
    height: 42px;
    border-radius: 50%;
    background: var(--terra);
    border: 2px solid var(--ink);
    color: var(--paper);
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 2px 2px 0 var(--ink);
    pointer-events: auto;
    cursor: ew-resize;
    transition: transform 0.3s var(--ease-spring);
  }

  .comparison-slider:hover .slider-handle {
    transform: scale(1.1);
  }

  .comparison-labels {
    display: flex;
    justify-content: space-between;
    gap: 24px;
  }

  .comparison-label {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    flex: 1;
  }

  .label-dot {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    flex-shrink: 0;
    margin-top: 6px;
  }

  .harder-dot {
    background: var(--ink-soft);
  }

  .smarter-dot {
    background: var(--terra);
  }

  .label-content {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .label-title {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.9rem;
    letter-spacing: 0.02em;
  }

  .label-desc {
    font-size: 0.78rem;
    color: var(--muted);
    line-height: 1.4;
    white-space: nowrap;
  }

  .slider-hint {
    text-align: center;
    margin-top: 18px;
    font-size: 1.2rem;
    color: var(--muted);
    transform: rotate(-1.5deg);
  }

  @media (max-width: 768px) {
    .comparison-labels {
      flex-direction: column;
      gap: 16px;
    }

    .label-smarter .label-content {
      text-align: left !important;
    }
  }

  @media (max-width: 480px) {
    .comparison-slider {
      height: 300px;
    }

    .label-desc {
      white-space: normal;
    }
  }
</style>

<script>
  const slider = document.getElementById('comparison-slider');
  const harderLayer = document.getElementById('slider-harder') as HTMLElement;
  const divider = document.getElementById('slider-divider') as HTMLElement;
  const labelHarder = document.getElementById('slider-label-harder') as HTMLElement;
  const labelSmarter = document.getElementById('slider-label-smarter') as HTMLElement;

  if (slider && harderLayer && divider) {
    let isDragging = false;

    // --- Whoosh: a short burst of filtered noise, synthesised on the fly ---
    let audioCtx: AudioContext | null = null;
    let lastWhoosh = 0;

    function whoosh(intensity: number) {
      const now = performance.now();
      if (now - lastWhoosh < 180) return;
      lastWhoosh = now;
      try {
        audioCtx = audioCtx || new AudioContext();
        if (audioCtx.state === 'suspended') audioCtx.resume().catch(() => {});
        const ctx = audioCtx;
        const dur = 0.28;
        const buffer = ctx.createBuffer(1, ctx.sampleRate * dur, ctx.sampleRate);
        const data = buffer.getChannelData(0);
        for (let i = 0; i < data.length; i++) data[i] = Math.random() * 2 - 1;

        const noise = ctx.createBufferSource();
        noise.buffer = buffer;

        const filter = ctx.createBiquadFilter();
        filter.type = 'bandpass';
        filter.Q.value = 1.1;
        filter.frequency.setValueAtTime(280, ctx.currentTime);
        filter.frequency.exponentialRampToValueAtTime(1600, ctx.currentTime + dur * 0.55);
        filter.frequency.exponentialRampToValueAtTime(420, ctx.currentTime + dur);

        const gain = ctx.createGain();
        gain.gain.setValueAtTime(0.0001, ctx.currentTime);
        gain.gain.exponentialRampToValueAtTime(0.14 * intensity, ctx.currentTime + 0.05);
        gain.gain.exponentialRampToValueAtTime(0.0001, ctx.currentTime + dur);

        noise.connect(filter);
        filter.connect(gain);
        gain.connect(ctx.destination);
        noise.start();
        noise.stop(ctx.currentTime + dur);
      } catch {
        // Audio blocked or unsupported — the slider stays silent, no harm done
      }
    }

    let lastSide: string | null = null;

    function updateSlider(clientX: number) {
      const rect = slider!.getBoundingClientRect();
      let x = clientX - rect.left;
      x = Math.max(0, Math.min(x, rect.width));
      const pct = (x / rect.width) * 100;

      harderLayer.style.clipPath = `inset(0 ${100 - pct}% 0 0)`;
      divider.style.left = `${pct}%`;

      // Show HARDER when mostly harder visible (>50%), SMARTER when mostly smarter
      const side = pct > 50 ? 'harder' : 'smarter';
      if (labelHarder && labelSmarter) {
        if (side === 'harder') {
          labelHarder.style.display = '';
          labelSmarter.style.display = 'none';
        } else {
          labelHarder.style.display = 'none';
          labelSmarter.style.display = '';
        }
      }

      // Whoosh as the divider sweeps past the middle
      if (lastSide && side !== lastSide) whoosh(1);
      lastSide = side;
    }

    slider.addEventListener('mousedown', (e) => {
      isDragging = true;
      whoosh(0.55);
      updateSlider(e.clientX);
    });

    window.addEventListener('mousemove', (e) => {
      if (!isDragging) return;
      e.preventDefault();
      updateSlider(e.clientX);
    });

    window.addEventListener('mouseup', () => {
      isDragging = false;
    });

    // Touch support
    slider.addEventListener('touchstart', (e) => {
      isDragging = true;
      whoosh(0.55);
      updateSlider(e.touches[0].clientX);
    }, { passive: true });

    window.addEventListener('touchmove', (e) => {
      if (!isDragging) return;
      updateSlider(e.touches[0].clientX);
    }, { passive: true });

    window.addEventListener('touchend', () => {
      isDragging = false;
    });
  }
</script>
